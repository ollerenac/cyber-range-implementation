---
phase: 01.5-perimeter-firewall
plan: 03
type: execute
wave: 3
depends_on: [01.5-02]
files_modified:
  - docs/index.md
  - docs/phase-01-runbook.md
  - docs/phase-02-runbook.md
  - docs/phase-03-runbook.md
autonomous: true
requirements: [INFRA-08, INFRA-09]

must_haves:
  truths:
    - "docs/index.md muestra la topología actualizada con OPNsense como gateway en 10.0.0.254 y el doble NAT (vmbr1→vmbr0→OPNsense→internet)"
    - "docs/phase-01-runbook.md incluye una sección Wave 1.5 / OPNsense con los pasos de setup del gateway perimetral"
    - "docs/phase-02-runbook.md explica la ruta de egress completa para VMs TARGET: vmbr1→host NAT→vmbr0→OPNsense→internet"
    - "docs/phase-03-runbook.md menciona Snort en OPNsense como tercera fuente de telemetría de red (complementando Packetbeat y Sysmon)"
    - "Los 5 criterios de éxito de Phase 1.5 pueden verificarse usando los comandos documentados en los runbooks"
  artifacts:
    - path: "docs/index.md"
      provides: "Topología actualizada con OPNsense VM en Host 6, diagrama ASCII de red corregido"
      contains: "10.0.0.254"
    - path: "docs/phase-01-runbook.md"
      provides: "Sección OPNsense gateway con pasos host6-firewall-setup.sh + deploy-opnsense-vm.sh + gates de verificación"
      contains: "OPNsense"
    - path: "docs/phase-02-runbook.md"
      provides: "Nota de ruta de egress TARGET VMs a través de OPNsense"
      contains: "OPNsense"
    - path: "docs/phase-03-runbook.md"
      provides: "Mención de Snort como telemetría perimetral complementaria"
      contains: "Snort"
  key_links:
    - from: "docs/index.md diagrama de topología"
      to: "OPNsense VM 10.0.0.254"
      via: "bloque ASCII o markdown actualizado"
      pattern: "10\\.0\\.0\\.254|OPNsense"
    - from: "docs/phase-01-runbook.md"
      to: "scripts/proxmox/host6-firewall-setup.sh y deploy-opnsense-vm.sh"
      via: "referencias de comandos bash en la sección OPNsense"
      pattern: "host6-firewall-setup\\.sh|deploy-opnsense-vm\\.sh"
---

<objective>
Actualizar los cuatro documentos de GitHub Pages para reflejar la topología con OPNsense como gateway perimetral del lab. Los docs deben estar en sincronía con la infraestructura real: cualquier operador que llegue al repo debe entender el flujo de red completo (incluyendo el doble NAT para VMs TARGET) sin consultar el ADR.

Purpose: Los docs de GitHub Pages son el artefacto público y reproducible del lab. Con OPNsense desplegado pero sin actualizar los runbooks, cualquier reproductor del lab tendría una topología documentada incorrecta (sin gateway, sin Snort) — invalidando DOCS-01 y DOCS-02 al final del proyecto.

Output: index.md con topología actualizada, phase-01-runbook.md con sección de setup OPNsense, phase-02-runbook.md con nota de egress TARGET, phase-03-runbook.md con Snort como fuente de telemetría.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/decisions/adr-firewall-opnsense.md
@.planning/phases/01.5-perimeter-firewall/01.5-01-SUMMARY.md
@.planning/phases/01.5-perimeter-firewall/01.5-02-SUMMARY.md
@docs/index.md
@docs/phase-01-runbook.md
@docs/phase-02-runbook.md
@docs/phase-03-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Actualizar docs/index.md — topología con OPNsense + diagrama de red corregido</name>
  <files>docs/index.md</files>
  <read_first>
    - docs/index.md completo (leer antes de editar) — el diagrama ASCII de red en líneas ~51-72 actualmente no incluye OPNsense; la sección "Phase Status" tiene numeración incorrecta (no incluye Phase 1.5 ni coincide con ROADMAP)
    - .planning/decisions/adr-firewall-opnsense.md — sección "Topología resultante" (bloque ASCII completo) y tabla "Flujo de tráfico por caso de uso" — usar como fuente canónica para el diagrama actualizado
  </read_first>
  <action>
    Editar docs/index.md para reflejar la topología con OPNsense. Cambios concretos:

    1. REEMPLAZAR el bloque "Lab Topology" (sección que empieza con "## Lab Topology" y el bloque de código ASCII). El nuevo diagrama debe mostrar:
       - Nivel físico: WiFi → Host 6 wlan0 → (kernel NAT) → vmbr_wan (172.16.0.0/30)
       - OPNsense VM en Host 6: WAN 172.16.0.2, LAN 10.0.0.254/24, Snort inline en LAN
       - MGMT Network 10.0.0.0/24 con default gateway 10.0.0.254 (OPNsense LAN)
       - TARGET Network 10.10.10.0/24: VMs Windows con egress vía host NAT → vmbr0 → OPNsense
       - Incluir nota: "VMs TARGET (10.10.10.x) alcanzan internet vía doble NAT: host kernel (vmbr1→vmbr0) + OPNsense (LAN→WAN)"

    2. AÑADIR OPNsense al listado de infraestructura en la sección "Infrastructure" del stack (actualmente no aparece):
       ```
       - **OPNsense** — gateway perimetral + Snort IDS inline (ET Open ruleset)
       ```

    3. ACTUALIZAR la tabla "Phase Status" para incluir Phase 1.5:
       ```
       | **01.5** | Perimeter Firewall (OPNsense) | Estado actual |
       ```
       Insertar la fila entre Phase 01 y Phase 02. Mantener los estados existentes de las demás fases.

    4. NO cambiar ninguna otra sección del archivo — solo las tres áreas descritas arriba.
  </action>
  <verify>
    <automated>grep -q '10.0.0.254' docs/index.md &amp;&amp; grep -qi 'opnsense' docs/index.md &amp;&amp; grep -q '1\.5' docs/index.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.md contiene la cadena "10.0.0.254" (IP LAN de OPNsense)
    - docs/index.md contiene "OPNsense" (al menos una vez, en sección Infrastructure o topology)
    - docs/index.md contiene "1.5" o "Perimeter Firewall" en la tabla de Phase Status
    - El diagrama de red actualizado muestra la ruta de egress de VMs TARGET (doble NAT)
    - Las demás secciones del archivo (APT Groups, Red Team stack, Operator Runbooks) permanecen sin cambios
  </acceptance_criteria>
  <done>docs/index.md refleja la topología con OPNsense como gateway 10.0.0.254, Phase 1.5 aparece en la tabla de estado, y el diagrama ASCII muestra el flujo de tráfico completo incluyendo doble NAT para TARGET.</done>
</task>

<task type="auto">
  <name>Tarea 2: Actualizar los tres runbooks de fases con secciones OPNsense y Snort</name>
  <files>docs/phase-01-runbook.md, docs/phase-02-runbook.md, docs/phase-03-runbook.md</files>
  <read_first>
    - docs/phase-01-runbook.md — leer completo para entender la estructura de secciones (Wave 1, Wave 2, etc.) y dónde insertar la sección de OPNsense. Actualmente termina en Wave 4 (CALDERA). La nueva sección "Phase 1.5 — OPNsense Gateway" va ANTES de Phase 2 (es una fase propia insertada entre Phase 1 y Phase 2).
    - docs/phase-02-runbook.md — leer la sección de topología de red (si existe) para añadir nota de egress TARGET
    - docs/phase-03-runbook.md — leer la sección de fuentes de telemetría para insertar Snort como tercera fuente
    - .planning/decisions/adr-firewall-opnsense.md — tabla "Flujo de tráfico por caso de uso" para la descripción de rutas
  </read_first>
  <action>
    Editar los tres runbooks. Cambios concretos en cada uno:

    **docs/phase-01-runbook.md:**
    Al final del archivo (después de Wave 4 — CALDERA), añadir una nueva sección H2:

    ```markdown
    ---

    ## Phase 1.5 — OPNsense Perimeter Gateway

    > **Prerequisito:** Host 6 debe tener `wlan0` asociado al WiFi del lab con IP asignada antes de ejecutar estos pasos.

    ### Paso 1 — Preparar red de Host 6

    ```bash
    # En Host 6 (ssh root@pve6):
    # Dry-run primero — revisar output antes de aplicar
    bash scripts/proxmox/host6-firewall-setup.sh
    bash scripts/proxmox/configure-vmbr-lan.sh eth0 --mode=new-bridge

    # Aplicar:
    bash scripts/proxmox/host6-firewall-setup.sh --apply
    bash scripts/proxmox/configure-vmbr-lan.sh eth0 --mode=new-bridge --apply
    ```

    Gates de verificación:
    ```bash
    iptables -t nat -L POSTROUTING -n | grep MASQUERADE   # debe mostrar regla sobre wlan0
    ip addr show vmbr_lan | grep '10.0.0.254'             # bridge LAN listo
    ```

    ### Paso 2 — Desplegar y instalar OPNsense VM

    ```bash
    # En Host 6:
    bash scripts/proxmox/deploy-opnsense-vm.sh --apply
    # Abrir consola noVNC de VMID 700 en Proxmox web UI y seguir el instalador
    ```

    Configuración post-instalación en https://10.0.0.254 (ver Plan 01.5-02 para pasos completos):
    - WAN: 172.16.0.2/30, gateway 172.16.0.1
    - LAN: 10.0.0.254/24, DHCP desactivado
    - NAT Outbound: Automático (LAN → WAN)
    - Snort: Plugin `os-snort`, interfaz LAN, reglas ET Open activadas

    ### Paso 3 — Verificar gateway completo

    ```bash
    # Desde elastic-vm (10.0.0.10):
    ping -c 3 8.8.8.8      # debe responder — tráfico vía OPNsense → wlan0 → internet

    # Desde kali o cualquier VM del lab:
    nmap -sS 10.0.0.1      # provoca alerta en OPNsense → Services → Intrusion Detection → Alerts
    ```
    ```

    (El bloque de código markdown interior debe estar correctamente cerrado — tres backticks anidados correctamente.)

    **docs/phase-02-runbook.md:**
    Localizar la sección de topología de red o la introducción del runbook. Añadir una nota (callout tipo `!!! note`) inmediatamente después del primer párrafo o de la sección "Lab Network Topology":

    ```markdown
    !!! note "Ruta de egress de VMs TARGET hacia internet"
        Las VMs del TARGET network (10.10.10.x: dc01, exchange01, sql01, ws01, ws02) alcanzan
        internet vía **doble NAT**:
        1. Host kernel (Proxmox): `vmbr1 → vmbr0` — reenvío L3 dentro del mismo host
        2. OPNsense (Host 6): `LAN (10.0.0.254) → WAN (172.16.0.2) → wlan0 → Internet`

        Default gateway de VMs TARGET: `10.10.10.1` (host kernel) → OPNsense → internet.
        Esta ruta es transparente para las VMs Windows — no requiere cambios en su configuración.
    ```

    **docs/phase-03-runbook.md:**
    Localizar la sección de fuentes de telemetría o la introducción que menciona Sysmon y Packetbeat. Añadir una nota sobre Snort como tercera fuente de telemetría perimetral:

    ```markdown
    !!! note "Telemetría perimetral — Snort en OPNsense"
        Además de Sysmon (host) y Packetbeat (red intra-segmento), el lab tiene una tercera
        fuente de telemetría de red:

        - **Snort IDS** en OPNsense (Host 6): monitoreo inline en la interfaz LAN (10.0.0.254)
        - Detecta: escaneos de red, C2 beaconing, movimiento lateral cross-segmento, DNS tunneling
        - Reglas activas: ET Open (Emerging Threats) — categorías malware, exploit, scan, trojan
        - Acceso a alertas: OPNsense web UI → Services → Intrusion Detection → Alerts
        - **Limitación**: Snort en OPNsense NO alimenta Elasticsearch directamente en esta fase.
          Los alertas se revisan manualmente en la web UI. Integración con Elastic via syslog
          es un ítem de Phase 7 (mejora futura).
    ```

    En los tres archivos: preservar exactamente el resto del contenido — solo añadir o modificar las secciones descritas. No reformatear secciones existentes.
  </action>
  <verify>
    <automated>grep -qi 'opnsense' docs/phase-01-runbook.md &amp;&amp; grep -qi 'opnsense' docs/phase-02-runbook.md &amp;&amp; grep -qi 'snort' docs/phase-03-runbook.md &amp;&amp; grep -q 'host6-firewall-setup' docs/phase-01-runbook.md &amp;&amp; grep -q '10.0.0.254' docs/phase-01-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/phase-01-runbook.md contiene "OPNsense" y "host6-firewall-setup.sh" y "10.0.0.254"
    - docs/phase-01-runbook.md contiene el comando `bash scripts/proxmox/deploy-opnsense-vm.sh --apply`
    - docs/phase-02-runbook.md contiene "OPNsense" y una explicación de la ruta doble NAT para VMs TARGET
    - docs/phase-03-runbook.md contiene "Snort" y menciona ET Open y la limitación (no integración directa con Elasticsearch)
    - Ninguno de los tres archivos tiene su contenido previo eliminado (solo adiciones/inserciones)
    - Los comandos bash en phase-01-runbook.md son copiables exactamente como están (sin parámetros placeholders entre `<>` sin explicación)
  </acceptance_criteria>
  <done>Los tres runbooks documentan el gateway OPNsense y Snort. Un operador que lea los docs puede reproducir la topología completa de Phase 1.5 sin consultar el ADR ni las notas de planificación.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| docs/ → GitHub Pages público | Los runbooks son documentos públicos. No deben contener credenciales, IPs privadas de producción, ni detalles de vulnerabilidades reales explotables fuera del contexto del lab. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.5-07 | Information Disclosure | docs/ publicados en GitHub Pages | accept | Las IPs del lab (10.0.0.x, 10.10.10.x) son rangos RFC1918 estándar sin valor para un atacante externo. Las credenciales van en .planning/secrets/PASSWORDS.md (no en docs/). Riesgo aceptado: el proyecto es un lab de investigación académica con intención pública. |
| T-1.5-SC | Tampering | Instalaciones de paquetes | n/a | Este plan solo modifica archivos markdown. No hay instalaciones de paquetes. |
</threat_model>

<verification>
- `grep -qi 'opnsense' docs/index.md` → 0 (match encontrado)
- `grep -q '10.0.0.254' docs/index.md` → 0
- `grep -qi 'opnsense' docs/phase-01-runbook.md` → 0
- `grep -q 'host6-firewall-setup' docs/phase-01-runbook.md` → 0
- `grep -qi 'opnsense' docs/phase-02-runbook.md` → 0
- `grep -qi 'snort' docs/phase-03-runbook.md` → 0
- Los cuatro archivos modificados no tienen errores de sintaxis Markdown que rompan el render de GitHub Pages
</verification>

<success_criteria>
- Un operador nuevo puede leer docs/phase-01-runbook.md y reproducir el setup completo de OPNsense en Host 6 usando solo los comandos documentados
- docs/index.md muestra la topología actualizada con OPNsense — ningún diagrama de red desactualizado permanece
- docs/phase-02-runbook.md explica por qué las VMs Windows tienen acceso a internet (doble NAT) — un operador no se confundirá pensando que la red TARGET es air-gapped en Phase 2+
- docs/phase-03-runbook.md menciona Snort como telemetría perimetral con la limitación honesta (sin integración Elasticsearch directa en esta fase)
</success_criteria>

<output>
Crear `.planning/phases/01.5-perimeter-firewall/01.5-03-SUMMARY.md` al completar. Registrar: secciones añadidas a cada runbook, verificación de que los comandos en los docs coinciden con los scripts reales (nombres de archivo y parámetros), y si se encontró algún contenido previo desactualizado que requirió corrección adicional.
</output>
