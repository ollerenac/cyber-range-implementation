---
phase: 01-proxmox-foundation-siem-node
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/elastic/install-stack.sh
  - scripts/elastic/elasticsearch.yml
  - scripts/elastic/jvm-heap.options
  - scripts/elastic/generate-certs.sh
  - scripts/elastic/kibana.yml
  - scripts/elastic/bootstrap-fleet.sh
  - scripts/elastic/ilm-policy.json
  - scripts/elastic/health-check.sh
autonomous: false
requirements: [INFRA-02]
user_setup:
  - service: elastic-stack
    why: "Elasticsearch superuser password and Kibana encryption keys are generated at first start and must be captured/stored by the operator"
    env_vars:
      - name: ES_PASSWORD
        source: "Elasticsearch first-start output (journalctl -u elasticsearch) or elasticsearch-reset-password -u elastic"

must_haves:
  truths:
    - "Elasticsearch 8.19.16 runs as a single-node cluster on 10.0.0.10:9200 with security enabled and cluster health green"
    - "Kibana 8.19.16 is reachable at https://10.0.0.10:5601 over TLS and logs in with the elastic superuser"
    - "A lab CA exists and a Fleet Server cert signed by it contains SAN IP:10.0.0.10"
    - "Fleet Server (via elastic-agent 8.19.16) is bootstrapped on 10.0.0.10:8220 and can issue an enrollment token"
    - "An ILM policy applies 30-day hot->delete retention to logs-* indices"
  artifacts:
    - path: "scripts/elastic/elasticsearch.yml"
      provides: "single-node ES config: network.host 10.0.0.10, discovery.type single-node, security enabled"
      contains: "discovery.type: single-node"
    - path: "scripts/elastic/jvm-heap.options"
      provides: "Xms8g/Xmx8g heap (D-08)"
      contains: "-Xms8g"
    - path: "scripts/elastic/generate-certs.sh"
      provides: "lab CA + Fleet/Kibana cert with IP:10.0.0.10 SAN (D-11/D-12)"
      min_lines: 20
    - path: "scripts/elastic/bootstrap-fleet.sh"
      provides: "elastic-agent Fleet Server install command (D-12/D-13)"
      min_lines: 15
    - path: "scripts/elastic/ilm-policy.json"
      provides: "30-day hot->delete ILM policy"
      contains: "30d"
  key_links:
    - from: "Fleet Server cert"
      to: "SAN IP:10.0.0.10"
      via: "elasticsearch-certutil cert --ip 10.0.0.10 (D-12)"
      pattern: "--ip 10.0.0.10"
    - from: "elastic-agent (Fleet Server)"
      to: "Elasticsearch https://10.0.0.10:9200"
      via: "--fleet-server-es with --fleet-server-es-ca"
      pattern: "fleet-server-es"
    - from: "Kibana"
      to: "Elasticsearch https://10.0.0.10:9200"
      via: "elasticsearch.hosts + lab CA"
      pattern: "https://10.0.0.10:9200"
---

<objective>
Install and harden the full SIEM stack on elastic-vm: Elasticsearch 8.19.16 (single-node, security enabled), Kibana 8.19.16 (HTTPS at 10.0.0.10:5601), a lab CA with a Fleet Server cert carrying SAN IP:10.0.0.10, Fleet Server bootstrapped to issue enrollment tokens, and a 30-day ILM policy. This delivers the bulk of INFRA-02 and ROADMAP Success Criteria 2 and 3.

Purpose: This is the persistent data sink and agent-management plane for the entire range. The single highest-risk artifact is the Fleet Server cert SAN — if it lacks IP:10.0.0.10, EVERY Phase 3 agent enrollment fails silently (T-1-03, RESEARCH Pitfall 1). The CA cert generated here is the trust anchor distributed to all target VMs in Phase 3.

Output: install script, ES/Kibana config drop-ins, cert-generation script, Fleet bootstrap script, ILM policy JSON, and a health-check script. After this plan, an operator can open Kibana, see green cluster health, and copy a valid enrollment token.

CRITICAL VERSION PIN: All three Elastic components install at EXACTLY 8.19.16 (elasticsearch=8.19.16 kibana=8.19.16 elastic-agent=8.19.16). This SUPERSEDES the 8.17.x in older CONTEXT.md/STATE.md — 8.17.x is EOL (Aug 5, 2025) per RESEARCH §State of the Art. STATE.md's rule "all Elastic components pinned to same 8.x patch" is honored; the patch is now 8.19.16. Mismatched versions between ES/Kibana/agent will break Kibana startup and Fleet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md
@.planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md
@.planning/phases/01-proxmox-foundation-siem-node/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Elasticsearch 8.19.16 install, config, heap, ILM policy + health-check scaffold</name>
  <files>scripts/elastic/install-stack.sh, scripts/elastic/elasticsearch.yml, scripts/elastic/jvm-heap.options, scripts/elastic/ilm-policy.json, scripts/elastic/health-check.sh</files>
  <read_first>
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Standard Stack" Installation block (lines ~138-158) — exact apt GPG key, 8.x repo, and `apt-get install elasticsearch=8.19.16 kibana=8.19.16 elastic-agent=8.19.16` pinned versions
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pattern 2" (lines ~304-338) — elasticsearch.yml keys (cluster.name, node.name, network.host 10.0.0.10, http.port 9200, discovery.type single-node) and jvm.options.d heap
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pattern 6" (lines ~437-465) — ILM policy JSON (30d delete) and §"Pitfall 6" (lines ~659-663) — heap must stay Xms8g/Xmx8g
    - .planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md D-08 (heap 8g/8g) and Claude's Discretion (ILM 30-day hot->delete)
    - .planning/phases/01-proxmox-foundation-siem-node/01-02-SUMMARY.md — confirm vm.max_map_count was set by Plan 02 cloud-init
  </read_first>
  <action>
    Create elasticsearch.yml drop-in: cluster.name cyber-range, node.name elastic-vm, network.host 10.0.0.10, http.port 9200, discovery.type: single-node, path.data /var/lib/elasticsearch, path.logs /var/log/elasticsearch. Leave security at the 8.x default (ENABLED) — do NOT set xpack.security.enabled: false; disabling it would make the SIEM world-readable (T-1-02).

    Create jvm-heap.options containing exactly the two lines `-Xms8g` and `-Xmx8g` (D-08), destined for /etc/elasticsearch/jvm.options.d/heap.options. Header comment: never exceed 31g (Pitfall 6); 8g is correct for the 14 GB elastic-vm.

    Create ilm-policy.json: hot phase rollover (max_age 1d, max_size 10gb), delete phase at min_age 30d (RESEARCH Pattern 6 + Claude's Discretion).

    Create install-stack.sh: adds the Elastic 8.x apt repo + GPG key; runs `apt-get install elasticsearch=8.19.16 kibana=8.19.16 elastic-agent=8.19.16` (version pin MANDATORY — header comment must state 8.17.x is EOL, 8.19.16 is the correct pin per STATE.md's same-patch rule); copies elasticsearch.yml and jvm-heap.options into place; asserts `sysctl vm.max_map_count` == 262144 (set by Plan 02 — fail loudly if not); enables+starts elasticsearch; instructs the operator to capture the first-start elastic password from `journalctl -u elasticsearch`; then PUTs the ILM policy as `lab-logs-30d` via curl --cacert and associates it with logs-* via an index template. Idempotent, `set -euo pipefail`, ES_PASSWORD read from env (never hardcoded — RESEARCH §Security Domain).

    Create health-check.sh (the INFRA-02 Wave 0 gate from VALIDATION.md): curls https://10.0.0.10:9200/_cluster/health --cacert and asserts status == green (exit non-zero otherwise); curls https://10.0.0.10:5601/api/status and asserts HTTP 200/302. ES_PASSWORD from env.

    Do NOT inline the full apt key/repo or curl bodies in this action — reference the RESEARCH Installation + Pattern 6 blocks. Pin 8.19.16 explicitly.
  </action>
  <verify>
    <automated>bash -n scripts/elastic/install-stack.sh && bash -n scripts/elastic/health-check.sh && grep -q '8.19.16' scripts/elastic/install-stack.sh && grep -q 'discovery.type: single-node' scripts/elastic/elasticsearch.yml && grep -q -- '-Xms8g' scripts/elastic/jvm-heap.options && python3 -c "import json,sys; d=json.load(open('scripts/elastic/ilm-policy.json')); sys.exit(0 if '30d' in json.dumps(d) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/elastic/install-stack.sh` and `bash -n scripts/elastic/health-check.sh` both exit 0
    - install-stack.sh pins all three packages to =8.19.16 (NOT 8.17.x) and contains a comment flagging the EOL version change
    - elasticsearch.yml has `network.host: 10.0.0.10` and `discovery.type: single-node`; does NOT contain `xpack.security.enabled: false`
    - jvm-heap.options contains `-Xms8g` and `-Xmx8g`
    - ilm-policy.json is valid JSON with a delete phase at min_age 30d
    - health-check.sh asserts cluster health == green (exit non-zero on any non-green)
  </acceptance_criteria>
  <done>Elasticsearch 8.19.16 config + install + ILM are scripted with security enabled, 8g heap, and a green-health gate. health-check.sh covers the INFRA-02 Wave 0 gate.</done>
</task>

<task type="auto">
  <name>Task 2: Generate lab CA + Fleet/Kibana certs (SAN IP:10.0.0.10) and configure Kibana HTTPS</name>
  <files>scripts/elastic/generate-certs.sh, scripts/elastic/kibana.yml</files>
  <read_first>
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pattern 3" (lines ~340-377) — exact elasticsearch-certutil ca / cert commands with `--ip 10.0.0.10 --dns fleet-server` and the openssl PEM export steps
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pitfall 1" (lines ~601-609) — SAN mismatch is the #1 Fleet failure; verify with `openssl x509 -text | grep "Subject Alternative"`
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pattern 5" (lines ~407-435) — kibana.yml keys + §"Open Questions" Q4 (lines ~791-794) — generate a SEPARATE Kibana cert (--dns elastic-vm --ip 10.0.0.10) in the same certutil run
    - .planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md D-11 (lab CA via certutil), D-12 (Fleet cert SAN IP:10.0.0.10, CN fleet-server/elastic-vm), D-13 (CA cert placed on elastic-vm for Phase 3 distribution)
  </read_first>
  <action>
    Create generate-certs.sh implementing RESEARCH Pattern 3 exactly: (1) `elasticsearch-certutil ca` → /etc/elasticsearch/certs/elastic-stack-ca.p12; (2) export CA as PEM ca.crt (this is the Phase 3 distribution trust anchor per D-13 — place at a documented path and chmod 644); (3) `elasticsearch-certutil cert --ca elastic-stack-ca.p12 --dns fleet-server --ip 10.0.0.10` → fleet-server.p12 (D-12 SAN is the load-bearing flag — T-1-03); (4) export fleet-server.crt and fleet-server.key as PEM (Fleet uses PEM, not p12); (5) generate a SEPARATE Kibana cert `--dns elastic-vm --ip 10.0.0.10` → kibana.crt/kibana.key (RESEARCH Q4). After generation, the script MUST self-verify the SAN: `openssl x509 -in fleet-server.crt -noout -text | grep -A1 "Subject Alternative"` and FAIL (exit non-zero) if `IP Address:10.0.0.10` is absent. Keys chmod 600. `set -euo pipefail`.

    Create kibana.yml (RESEARCH Pattern 5): server.port 5601, server.host 10.0.0.10, server.name cyber-range-kibana, elasticsearch.hosts ["https://10.0.0.10:9200"], server.ssl.enabled true with server.ssl.certificate/key pointing at the kibana.crt/key from generate-certs.sh, elasticsearch.ssl.certificateAuthorities pointing at the lab ca.crt, elasticsearch.ssl.verificationMode full. Note in a comment that Kibana encryption keys (xpack.*.encryptionKey) should be generated via `kibana-encryption-keys generate` and added by the operator (user_setup).

    Do NOT inline full cert command output — reference Pattern 3. The `--ip 10.0.0.10` flag and the post-generation SAN self-check are non-negotiable.
  </action>
  <verify>
    <automated>bash -n scripts/elastic/generate-certs.sh && grep -q -- '--ip 10.0.0.10' scripts/elastic/generate-certs.sh && grep -q 'Subject Alternative' scripts/elastic/generate-certs.sh && grep -q 'server.ssl.enabled: true' scripts/elastic/kibana.yml && grep -q 'https://10.0.0.10:9200' scripts/elastic/kibana.yml</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/elastic/generate-certs.sh` exits 0
    - generate-certs.sh passes `--ip 10.0.0.10` to `elasticsearch-certutil cert` for the Fleet Server cert (D-12)
    - generate-certs.sh self-verifies the SAN via `openssl x509 ... grep "Subject Alternative"` and fails if IP:10.0.0.10 is absent (T-1-03 mitigation)
    - generate-certs.sh exports a PEM ca.crt at a documented path for Phase 3 distribution (D-13)
    - kibana.yml has server.ssl.enabled: true, server.host 10.0.0.10, elasticsearch.hosts https://10.0.0.10:9200, and trusts the lab ca.crt
  </acceptance_criteria>
  <done>Lab CA + Fleet/Kibana certs are scripted with a self-verifying IP:10.0.0.10 SAN; Kibana is configured for HTTPS at 10.0.0.10:5601 trusting the lab CA. CA cert is placed for Phase 3 distribution.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap Fleet Server (8.19.16) via elastic-agent with the lab certs</name>
  <files>scripts/elastic/bootstrap-fleet.sh</files>
  <read_first>
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pattern 4" (lines ~379-405) — exact `elasticsearch-service-tokens create elastic/fleet-server` + `elastic-agent install --url=https://10.0.0.10:8220 --fleet-server-es=https://10.0.0.10:9200 --fleet-server-cert/--fleet-server-cert-key/--certificate-authorities` flags
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Fleet Server Health Check" (lines ~699-709) — `elastic-agent status` + `ss -tlnp | grep 8220` expected output
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Anti-Patterns" (line ~581) — Fleet Server must be healthy BEFORE any Phase 3 agent enrollment; and §"Security Domain" — store the service token in /root/.elastic-tokens chmod 600, never echo to stdout
    - .planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md D-12/D-13 (Fleet cert + CA), specifics (Fleet Server port 8220)
    - scripts/elastic/generate-certs.sh (Task 2) — the cert/key/ca paths this script consumes
  </read_first>
  <action>
    Create bootstrap-fleet.sh implementing RESEARCH Pattern 4: (1) create the Fleet service token via `elasticsearch-service-tokens create elastic/fleet-server fleet-token`, writing it to /root/.elastic-tokens (chmod 600, NEVER echoed to stdout — RESEARCH §Security Domain); (2) run `elastic-agent install` with --url=https://10.0.0.10:8220, --fleet-server-es=https://10.0.0.10:9200, --fleet-server-service-token from the stored file, --fleet-server-policy=fleet-server-policy, --fleet-server-es-ca / --certificate-authorities = the lab ca.crt, --fleet-server-cert / --fleet-server-cert-key = the PEM fleet-server.crt/key from Task 2, --fleet-server-port=8220. The elastic-agent binary is the 8.19.16 package installed in Task 1 (version-matched — Anti-Pattern: never mix versions). (3) Verify: `elastic-agent status` shows Fleet Server running + connected to ES, and `ss -tlnp | grep 8220` shows LISTEN on 10.0.0.10:8220 — fail the script if either check fails.

    Header comment: enrollment tokens issued by this Fleet Server must remain valid into Phase 3 (CONTEXT integration point); the operator copies a token from Kibana → Fleet → Enrollment Tokens (manual, covered in the checkpoint). `set -euo pipefail`.

    Do NOT inline the full install command's secret values — reference Pattern 4 and read paths from the generate-certs.sh outputs.
  </action>
  <verify>
    <automated>bash -n scripts/elastic/bootstrap-fleet.sh && grep -q 'https://10.0.0.10:8220' scripts/elastic/bootstrap-fleet.sh && grep -q 'fleet-server-es' scripts/elastic/bootstrap-fleet.sh && grep -q 'fleet-server-cert' scripts/elastic/bootstrap-fleet.sh && grep -q '8220' scripts/elastic/bootstrap-fleet.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/elastic/bootstrap-fleet.sh` exits 0
    - bootstrap-fleet.sh uses --url=https://10.0.0.10:8220 and --fleet-server-es=https://10.0.0.10:9200
    - bootstrap-fleet.sh passes --fleet-server-cert/--fleet-server-cert-key (the SAN-verified PEM certs from Task 2) and --certificate-authorities = lab ca.crt
    - the service token is written to a chmod 600 file and never echoed to stdout (T-1-02 / leakage mitigation)
    - the script verifies `ss -tlnp | grep 8220` LISTEN and fails otherwise
  </acceptance_criteria>
  <done>Fleet Server is scripted to bootstrap on 10.0.0.10:8220 using the SAN-verified lab certs, with the service token stored securely and a port-listen verification gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Stand up SIEM stack and confirm Kibana/Fleet/SAN gates</name>
  <action>This is a human-verification checkpoint — no code is written here. The operator performs the steps in <how-to-verify> on live hardware and confirms each gate. Do not auto-advance; wait for the resume-signal.</action>
  <what-built>
    Full SIEM stack scripts: ES 8.19.16 install + config + 8g heap + 30-day ILM, lab CA + SAN-verified Fleet/Kibana certs, Kibana HTTPS config, and Fleet Server bootstrap. Claude cannot run these on elastic-vm — the operator executes them in order and confirms the live success gates (ROADMAP criteria 2 and 3).
  </what-built>
  <how-to-verify>
    On elastic-vm (10.0.0.10), in order:
    1. `sudo bash scripts/elastic/install-stack.sh` — capture the elastic password from `journalctl -u elasticsearch`; export ES_PASSWORD.
    2. `sudo bash scripts/elastic/generate-certs.sh` — confirm it prints `IP Address:10.0.0.10` in the SAN self-check and exits 0 (if it fails here, the cert is wrong — STOP; this is T-1-03).
    3. Copy kibana.yml + the kibana cert into place, start kibana, then `sudo bash scripts/elastic/bootstrap-fleet.sh`.
    4. Run `bash scripts/elastic/health-check.sh` — must report cluster health GREEN and Kibana reachable.
    5. Browser: open https://10.0.0.10:5601, accept the lab-CA cert warning, log in as elastic — Kibana loads (ROADMAP criterion 2).
    6. In Kibana → Stack Management → Fleet → Agents: Fleet Server shows "Healthy". Go to Enrollment Tokens → "Create enrollment token" → confirm a token is copyable (ROADMAP criterion 3).
    7. Confirm SAN on the wire: `openssl s_client -connect 10.0.0.10:8220 </dev/null 2>/dev/null | openssl x509 -noout -text | grep -A1 "Subject Alternative"` shows IP Address:10.0.0.10.
    Report: cluster health status, the Fleet Server health state, and the openssl SAN line.
  </how-to-verify>
  <resume-signal>Type "approved" once cluster health is green, Kibana loads at https://10.0.0.10:5601, Fleet Server is Healthy with a copyable enrollment token, and the cert SAN shows IP:10.0.0.10 — or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → Kibana (10.0.0.10:5601) | Authenticated, TLS-only access to the SIEM UI. |
| Phase 3 Elastic Agents → Fleet Server (10.0.0.10:8220) | TLS, cert-pinned to the lab CA; SAN must match the IP agents connect by. |
| Kibana / Fleet Server → Elasticsearch (10.0.0.10:9200) | TLS with lab CA; service-token / superuser auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information Disclosure | Elasticsearch / Kibana exposure | mitigate | xpack.security stays ENABLED (default 8.x — never set to false in elasticsearch.yml); elastic superuser password captured + stored by operator, never hardcoded; Kibana served HTTPS-only (server.ssl.enabled: true). |
| T-1-03 | Tampering / Spoofing | Fleet Server TLS cert SAN | mitigate | generate-certs.sh passes `--ip 10.0.0.10` and self-verifies the SAN via openssl, failing if IP:10.0.0.10 is absent. Checkpoint re-verifies on the wire with `openssl s_client`. Without this, all Phase 3 enrollment fails silently (RESEARCH Pitfall 1). |
| T-1-leak | Information Disclosure | service token / enrollment token | mitigate | Fleet service token written to /root/.elastic-tokens chmod 600, never echoed to stdout; ES_PASSWORD read from env, never committed (RESEARCH §Security Domain). |
| T-1-SC | Tampering | apt installs (elasticsearch/kibana/elastic-agent 8.19.16) | accept | Installed from the official elastic.co vendor apt repo (8.x), GPG-key verified. RESEARCH §Package Legitimacy Audit: all three Approved (vendor-controlled repo), none [ASSUMED]/[SUS]/[SLOP] — no blocking human checkpoint required. |
</threat_model>

<verification>
- `curl -sk -u elastic:$ES_PASSWORD https://10.0.0.10:9200/_cluster/health?pretty` → status green, number_of_nodes 1
- `curl -sk -o /dev/null -w "%{http_code}" https://10.0.0.10:5601` → 200 or 302
- `elastic-agent status` → Fleet Server running, connected to ES; `ss -tlnp | grep 8220` → LISTEN 10.0.0.10:8220
- `openssl s_client -connect 10.0.0.10:8220 | openssl x509 -text | grep "Subject Alternative"` → IP Address:10.0.0.10
- ILM policy present: `curl -sk -u elastic:$ES_PASSWORD https://10.0.0.10:9200/_ilm/policy/lab-logs-30d`
</verification>

<success_criteria>
- ROADMAP Success Criterion 2 (Kibana loads at https://10.0.0.10:5601, ES health green) is TRUE
- ROADMAP Success Criterion 3 (Fleet UI shows active Fleet Server with a valid enrollment token) is TRUE
- Fleet Server cert SAN contains IP:10.0.0.10 (T-1-03 closed; Phase 3 enrollment will not fail on SAN)
- Lab CA cert (ca.crt) is placed at a documented path on elastic-vm for Phase 3 distribution (D-13)
</success_criteria>

<output>
Create `.planning/phases/01-proxmox-foundation-siem-node/01-03-SUMMARY.md` when done. Record: exact 8.19.16 patch installed (re-verify live), the documented path of the lab ca.crt for Phase 3, the verified cert SAN line, and where the elastic superuser password / service token are stored. Handoff note for Phase 3: the enrollment token and ca.crt path agents will need.
</output>
