---
phase: 01-proxmox-foundation-siem-node
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - scripts/caldera/install-caldera.sh
  - scripts/caldera/local.yml
  - scripts/caldera/caldera.service
  - scripts/proxmox/snapshot-test.sh
  - scripts/proxmox/reset_range.sh
autonomous: false
requirements: [INFRA-01]

must_haves:
  truths:
    - "CALDERA 5.3.0 is installed on caldera-vm (10.0.0.20) and the UI is reachable at http://10.0.0.20:8888"
    - "CALDERA conf/local.yml sets host 0.0.0.0, port 8888, app.contact.http http://10.0.0.20:8888, and app.contact.tcp 0.0.0.0:8853 explicitly (A1 honored)"
    - "Default CALDERA API keys (ADMIN123/BLUEADMIN123) are changed before first start (T-1-04)"
    - "qm snapshot + qm rollback are verified working on ONE throwaway test VM"
    - "reset_range.sh scaffold exists and EXCLUDES elastic-vm and caldera-vm by construction (D-10)"
  artifacts:
    - path: "scripts/caldera/install-caldera.sh"
      provides: "git clone 5.3.0 + pip install + systemd setup on caldera-vm"
      min_lines: 20
    - path: "scripts/caldera/local.yml"
      provides: "CALDERA config: 0.0.0.0:8888, contact.http 10.0.0.20:8888, contact.tcp 8853, changed API keys"
      contains: "app.contact.tcp"
    - path: "scripts/proxmox/reset_range.sh"
      provides: "parallel qm rollback scaffold for Phase 4+; elastic/caldera excluded"
      min_lines: 20
  key_links:
    - from: "CALDERA agents (Phase 4)"
      to: "http://10.0.0.20:8888"
      via: "app.contact.http in local.yml (Pitfall 5: must be routable IP, not 0.0.0.0)"
      pattern: "app.contact.http: http://10.0.0.20:8888"
    - from: "reset_range.sh"
      to: "target VMIDs only"
      via: "explicit allowlist that excludes elastic-vm + caldera-vm (D-10)"
      pattern: "qm rollback"
---

<objective>
Complete Phase 1: install CALDERA 5.3.0 on caldera-vm, verify the snapshot/rollback workflow on a throwaway VM, scaffold reset_range.sh (with elastic-vm + caldera-vm excluded by construction), and confirm all four ROADMAP success gates. This finishes the INFRA-01 success criteria and stands up the C2 platform for Phase 4.

Purpose: CALDERA is the persistent red-team platform; its agent contact URL must be a routable IP (10.0.0.20), not 0.0.0.0 (Pitfall 5), or Phase 4 agents will never beacon. The snapshot workflow is the foundation of the project's core value ("reset everything in one command"), and reset_range.sh MUST never touch elastic-vm (the data sink) or caldera-vm (D-10) — encoding that exclusion now prevents a catastrophic Phase 3 mistake.

Output: CALDERA install script + config + systemd unit, a snapshot-test script, the reset_range.sh scaffold, and confirmation of all 4 Phase 1 gates.

PORT NOTE (RESEARCH A1): CONTEXT.md states the CALDERA TCP beacon is 8853, but CALDERA 5.x defaults to app.contact.tcp 7010. Per planning guidance, local.yml MUST set `app.contact.tcp: 0.0.0.0:8853` EXPLICITLY to honor the CONTEXT decision, and the install must verify which ports actually listen (`ss -tlnp | grep -E '8888|8853|7010'`). Sandcat agents beacon over HTTP (app.contact.http) to 10.0.0.20:8888 by default — document the actual beacon path in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md
@.planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md
@.planning/phases/01-proxmox-foundation-siem-node/01-02-SUMMARY.md
@.planning/phases/01-proxmox-foundation-siem-node/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CALDERA 5.3.0 install script, local.yml config (8853 explicit + changed keys), systemd unit</name>
  <files>scripts/caldera/install-caldera.sh, scripts/caldera/local.yml, scripts/caldera/caldera.service</files>
  <read_first>
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pattern 7" (lines ~467-522) — git clone --branch 5.3.0, pip3 install, python3 server.py --insecure --build, conf/local.yml keys, systemd unit pattern
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Open Questions" Q1 (lines ~776-779) + Assumptions A1/A2/A3 (lines ~762-765) — 8853 vs 7010 port discrepancy; set 8853 explicitly + verify open ports
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pitfall 5" (lines ~651-657) — app.contact.http MUST be http://10.0.0.20:8888 (routable IP, not 0.0.0.0) or agents can't beacon
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Security Domain" (line ~881) — change api_key_red/api_key_blue from defaults (T-1-04)
    - .planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md specifics (CALDERA UI 8888, beacon 8853, caldera-vm 10.0.0.20)
    - .planning/phases/01-proxmox-foundation-siem-node/01-02-SUMMARY.md — caldera-vm VMID and confirmed 10.0.0.20 reachability
  </read_first>
  <action>
    Create local.yml (CALDERA conf/local.yml override) per RESEARCH Pattern 7 with these EXACT values: host: 0.0.0.0, port: 8888, app.contact.http: http://10.0.0.20:8888 (routable IP per Pitfall 5 — NOT 0.0.0.0), app.contact.tcp: 0.0.0.0:8853 (EXPLICIT per A1 / planning guidance — overrides the 7010 default to honor the CONTEXT 8853 decision), app.contact.udp and websocket left at defaults. CHANGE api_key_red and api_key_blue away from the documented defaults ADMIN123/BLUEADMIN123 (T-1-04) — use placeholder tokens the operator must replace, with a header comment instructing rotation. Keep the users block minimal (operator sets real creds).

    Create install-caldera.sh: `git clone https://github.com/mitre/caldera.git --recursive --branch 5.3.0` into /opt/caldera, `pip3 install -r requirements.txt`, copy local.yml into conf/, run `python3 server.py --insecure --build` once to compile the VueJS UI (--build required on first run per Anti-Patterns), then install + enable the systemd unit. After start, verify: `curl -s http://10.0.0.20:8888/api/v2/health` returns version 5.3.0, AND `ss -tlnp | grep -E '8888|8853|7010'` — DOCUMENT which ports actually listen (this resolves A1). Fail the script if the UI health endpoint is unreachable. `set -euo pipefail`. CALDERA is installed from github.com/mitre/caldera (MITRE — Approved per RESEARCH §Package Legitimacy Audit; no [ASSUMED]/[SUS] git source).

    Create caldera.service (systemd unit) per RESEARCH Pattern 7: Type simple, User caldera, WorkingDirectory /opt/caldera, ExecStart python3 server.py --insecure, Restart always, PYTHONUNBUFFERED=1, WantedBy multi-user.target.

    Do NOT inline the full requirements install output — reference Pattern 7. The 8853 explicit setting and the API-key change are non-negotiable.
  </action>
  <verify>
    <automated>bash -n scripts/caldera/install-caldera.sh && grep -q '5.3.0' scripts/caldera/install-caldera.sh && grep -q 'app.contact.tcp' scripts/caldera/local.yml && grep -q 'app.contact.http: http://10.0.0.20:8888' scripts/caldera/local.yml && ! grep -q 'ADMIN123' scripts/caldera/local.yml && grep -q 'multi-user.target' scripts/caldera/caldera.service</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/caldera/install-caldera.sh` exits 0
    - install-caldera.sh clones CALDERA at `--branch 5.3.0` and runs `server.py --insecure --build` on first run
    - install-caldera.sh verifies http://10.0.0.20:8888/api/v2/health and greps `ss -tlnp` for 8888/8853/7010 to document actual ports (A1)
    - local.yml sets app.contact.http to http://10.0.0.20:8888 (routable IP, Pitfall 5) and app.contact.tcp to 0.0.0.0:8853 (explicit, A1)
    - local.yml does NOT contain the default keys ADMIN123/BLUEADMIN123 (T-1-04)
    - caldera.service is a valid systemd unit targeting multi-user.target
  </acceptance_criteria>
  <done>CALDERA 5.3.0 install + config + systemd are scripted with the 8853 contact port explicit, routable beacon URL, changed API keys, and a UI-health verification gate.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot-test script + reset_range.sh scaffold (elastic/caldera excluded)</name>
  <files>scripts/proxmox/snapshot-test.sh, scripts/proxmox/reset_range.sh</files>
  <read_first>
    - .planning/phases/01-proxmox-foundation-siem-node/01-RESEARCH.md §"Pattern 9" (lines ~543-572) — qm snapshot / qm rollback / qm delsnapshot syntax; NO --quiesce flag (guest-agent freeze is automatic when agent enabled)
    - .planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md D-05 (one snapshot clean_state, qm rollback is the reset command), D-10 (elastic-vm + caldera-vm NEVER snapshotted / NEVER in reset_range.sh — HARD constraint), D-NEW-09 (host/VM layout)
    - .planning/STATE.md Critical Constraints — `qm` not `virsh`; snapshots simultaneous across VMs after agents Healthy; CALDERA agent binary not present at snapshot time
    - .planning/phases/01-proxmox-foundation-siem-node/01-VALIDATION.md "Manual-Only Verifications" — qm snapshot/rollback/delsnapshot test sequence
  </read_first>
  <action>
    Create snapshot-test.sh: takes a throwaway test VMID as $1, runs `qm snapshot <VMID> test_snap`, then `qm rollback <VMID> test_snap`, then `qm delsnapshot <VMID> test_snap`, timing the rollback and asserting it completes successfully. This verifies the D-05 reset mechanism on ONE VM (per ROADMAP / Open Question on qm snapshot reliability) WITHOUT touching elastic-vm or caldera-vm. Use `qm` exclusively (never virsh — STATE.md). `set -euo pipefail`.

    Create reset_range.sh as a SCAFFOLD for Phase 3/4+ (full target VMs do not exist yet): it defines an explicit RESET_VMS allowlist mapping host→VMID for the reset-cycle VMs (dc01, exchange01, sql01, ws01, ws02, kali — to be filled in Phase 2/3), and for each runs parallel-over-SSH `qm stop && qm rollback <VMID> clean_state && qm start`. CRITICAL (D-10): the script MUST contain an explicit guard that REFUSES to operate on elastic-vm (10.0.0.10) or caldera-vm (10.0.0.20) — include a hardcoded EXCLUDE list and a runtime check that aborts if any VMID resolves to those VMs. Add a prominent header comment: "elastic-vm and caldera-vm are NEVER in this script (D-10, STATE.md) — they are the persistent data sink and C2 platform." Since target VMIDs are unknown in Phase 1, leave the allowlist as documented placeholders with a TODO referencing Phase 3 RESET-02; the parallel-rollback structure and the exclusion guard are the deliverable now.

    Do NOT inline a full parallel-SSH framework — a clear loop with `&` + `wait` and the exclusion guard suffices. Reference RESEARCH Pattern 9 for qm syntax.
  </action>
  <verify>
    <automated>bash -n scripts/proxmox/snapshot-test.sh && bash -n scripts/proxmox/reset_range.sh && grep -q 'qm rollback' scripts/proxmox/reset_range.sh && grep -qi 'elastic-vm' scripts/proxmox/reset_range.sh && grep -qi 'caldera-vm' scripts/proxmox/reset_range.sh && ! grep -q 'virsh' scripts/proxmox/reset_range.sh && ! grep -q 'virsh' scripts/proxmox/snapshot-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/proxmox/snapshot-test.sh` and `bash -n scripts/proxmox/reset_range.sh` both exit 0
    - snapshot-test.sh uses `qm snapshot`/`qm rollback`/`qm delsnapshot` (never virsh)
    - reset_range.sh contains an explicit guard naming elastic-vm and caldera-vm as EXCLUDED (D-10) and aborts if a target resolves to them
    - reset_range.sh uses `qm rollback <VMID> clean_state` and parallel execution (`&` + `wait`)
    - neither script references virsh (STATE.md: qm only)
  </acceptance_criteria>
  <done>Snapshot/rollback workflow is scripted and testable on a throwaway VM; reset_range.sh scaffold exists with the D-10 exclusion of elastic-vm + caldera-vm baked in by construction.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Install CALDERA, verify snapshots, confirm all 4 Phase 1 gates</name>
  <action>This is a human-verification checkpoint — no code is written here. The operator performs the steps in <how-to-verify> on live hardware and confirms each gate. Do not auto-advance; wait for the resume-signal.</action>
  <what-built>
    CALDERA 5.3.0 install + config (8853 explicit, changed keys), snapshot-test script, and reset_range.sh scaffold with elastic/caldera excluded. Claude cannot run these on caldera-vm or the Proxmox hosts — the operator executes them and confirms the final Phase 1 gates plus the snapshot workflow.
  </what-built>
  <how-to-verify>
    On caldera-vm (10.0.0.20):
    1. `sudo bash scripts/caldera/install-caldera.sh` (after rotating the API-key placeholders in local.yml). Confirm `curl -s http://10.0.0.20:8888/api/v2/health` returns version 5.3.0 and the CALDERA UI loads in a browser at http://10.0.0.20:8888.
    2. Run `ss -tlnp | grep -E '8888|8853|7010'` and REPORT which ports actually listen (resolves A1 — note in approval whether 8853 or 7010 is the live TCP contact).
    On any Proxmox host with a throwaway test VM:
    3. `bash scripts/proxmox/snapshot-test.sh <test-VMID>` — confirm snapshot → rollback → delsnapshot all succeed and rollback time is reported.
    4. Inspect reset_range.sh: confirm it explicitly excludes elastic-vm (10.0.0.10) and caldera-vm (10.0.0.20).
    FINAL PHASE 1 GATE (all four must pass):
    5. SSH any host → `brctl show vmbr0 vmbr1`; vmbr1 has no physical uplink (criterion 1).
    6. https://10.0.0.10:5601 → Kibana loads, ES health green (criterion 2).
    7. Fleet UI → active Fleet Server, enrollment token copyable (criterion 3).
    8. Test VM on vmbr1 cannot ping 8.8.8.8 or any LAN host (criterion 4).
    Report: CALDERA version + live contact ports, snapshot-test result, and the 4 gate outcomes.
  </how-to-verify>
  <resume-signal>Type "approved" once CALDERA is reachable at http://10.0.0.20:8888, the snapshot workflow succeeds, reset_range.sh excludes the persistent VMs, and all 4 ROADMAP Phase 1 gates pass — or describe which failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator / agents → CALDERA (10.0.0.20:8888 UI, :8853 TCP contact) | C2 control plane; default API keys would grant red/blue access to anyone on MGMT. |
| reset_range.sh (control node) → target VM disks via qm rollback | Destructive operation; must never reach the persistent data sink (elastic-vm) or C2 (caldera-vm). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Spoofing / EoP / Information Disclosure | CALDERA default API keys + LAN reachability | mitigate | local.yml changes api_key_red/api_key_blue from the documented defaults (ADMIN123/BLUEADMIN123) before first start; CALDERA is on MGMT only (vmbr0), not on the TARGET attack network or the public LAN beyond management. app.contact.http pinned to the routable MGMT IP. |
| T-1-data | Tampering / Denial of Service | reset_range.sh rolling back the SIEM | mitigate | reset_range.sh hardcodes an EXCLUDE guard for elastic-vm (10.0.0.10) and caldera-vm (10.0.0.20) and aborts if a target resolves to them (D-10) — prevents destroying the FullAPT-2025 corpus. |
| T-1-snap | Denial of Service | qm snapshot corruption | accept (Phase 1) | Snapshot workflow validated on a throwaway VM only this phase; Windows-VM guest-agent freeze consistency is a Phase 3 concern (RESEARCH Pattern 9 / STATE Open Question). |
| T-1-SC | Tampering | CALDERA git clone + pip install | accept | CALDERA cloned from github.com/mitre/caldera (MITRE — Approved per RESEARCH §Package Legitimacy Audit); pip requirements are CALDERA's own pinned set. No public-registry [ASSUMED]/[SUS]/[SLOP] packages introduced — no blocking human checkpoint required. |
</threat_model>

<verification>
- `curl -s http://10.0.0.20:8888/api/v2/health | python3 -m json.tool` → version 5.3.0
- `ss -tlnp | grep -E '8888|8853|7010'` on caldera-vm → 8888 listening; document TCP contact port
- `qm snapshot <test-VMID> test_snap && qm rollback <test-VMID> test_snap && qm delsnapshot <test-VMID> test_snap` → all succeed
- reset_range.sh: `grep -i 'elastic-vm\|caldera-vm' scripts/proxmox/reset_range.sh` shows the exclusion guard
- All 4 ROADMAP Phase 1 success criteria pass (see checkpoint steps 5-8)
</verification>

<success_criteria>
- CALDERA 5.3.0 reachable at http://10.0.0.20:8888 with non-default API keys
- ROADMAP Success Criterion 1 (bridges + vmbr1 no uplink) re-confirmed on the wire
- ROADMAP Success Criterion 4 (vmbr1 test VM cannot reach 8.8.8.8/LAN) re-confirmed
- qm snapshot/rollback verified on a throwaway VM (D-05 mechanism proven)
- reset_range.sh scaffold excludes elastic-vm + caldera-vm (D-10) — all 4 Phase 1 gates TRUE
</success_criteria>

<output>
Create `.planning/phases/01-proxmox-foundation-siem-node/01-04-SUMMARY.md` when done. Record: the LIVE CALDERA TCP contact port (8853 vs 7010 — resolves A1), the snapshot rollback time observed, the test VMID used, and a final checklist of all 4 ROADMAP Phase 1 gates with pass/fail. Handoff note for Phase 3: reset_range.sh allowlist must be populated with target VMIDs once dc01/exchange01/sql01/ws01/ws02/kali exist.
</output>
