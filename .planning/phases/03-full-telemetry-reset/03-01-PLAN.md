---
phase: 03-full-telemetry-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/proxmox/create-kali-vm.sh
autonomous: false
requirements:
  - INFRA-07

must_haves:
  truths:
    - "Kali VM (VMID 601) exists on Host 6 with dual NICs: vmbr1 (TARGET 10.10.10.200/24) and vmbr0 (MGMT 10.0.0.16/24)"
    - "SSH from elastic-vm reaches kali@10.0.0.16 without a password"
    - "elastic-vm has passwordless SSH to root@host2, root@host3, root@host4, root@host6 (Proxmox hosts)"
    - "Kali credentials changed from default kali/kali before clean_state snapshot"
    - "BloodHound CE docker-compose stack running on Kali; Mimikatz PE staged at /opt/mimikatz/"
    - "Metasploit, Nmap, and Impacket reachable from Kali CLI"
  artifacts:
    - path: "scripts/proxmox/create-kali-vm.sh"
      provides: "qm create + qm importdisk for Kali VMID 601 on Host 6"
      contains: "VMID=601"
  key_links:
    - from: "elastic-vm (10.0.0.10)"
      to: "kali (10.0.0.16)"
      via: "SSH over MGMT NIC (vmbr0)"
      pattern: "ssh kali@10.0.0.16"
    - from: "elastic-vm (10.0.0.10)"
      to: "Proxmox host6"
      via: "SSH root@host6 (passwordless ed25519)"
      pattern: "ssh root@host6 qm"
---

<objective>
Provision Kali Linux VM (VMID 601) on Host 6 and establish the SSH infrastructure that
reset_range.sh and take-snapshot.sh depend on. Also configures Kali post-import tools
(BloodHound CE, Mimikatz staging) and verifies INFRA-07 requirements.

Purpose: Kali must exist before Phase 3 agent enrollment; the elastic-vm SSH keys to
all four Proxmox hosts (host2/3/4/6) must be in place before reset_range.sh can fan
out in Wave 4. Both are blocking prerequisites with no mutual dependency, but Kali
provisioning and SSH key setup are authored in one plan to keep the human-interactive
console steps together.

Output:
- scripts/proxmox/create-kali-vm.sh (new file)
- Kali VM running on Host 6, MGMT IP 10.0.0.16 reachable from elastic-vm
- elastic-vm has passwordless SSH to root@host2, root@host3, root@host4, root@host6
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-telemetry-reset/03-CONTEXT.md
@.planning/phases/03-full-telemetry-reset/03-RESEARCH.md
@.planning/phases/01-proxmox-foundation-siem-node/01-CONTEXT.md

<interfaces>
<!-- Key patterns extracted from existing scripts -->

From scripts/proxmox/create-caldera-vm.sh (template to follow):
- Argument signature: VMID LAN_GW STORAGE CLOUD_IMAGE SSH_KEYFILE
- qm create flags: --name --memory --cores --cpu cputype=host --net0 --ostype l26 --agent 1 --onboot 1 --scsihw virtio-scsi-pci
- qm importdisk VMID IMAGE STORAGE
- qm set VMID --scsi0 "${STORAGE}:vm-${VMID}-disk-0,discard=on,ssd=1" --boot "order=scsi0" --bootdisk scsi0
- qm resize VMID scsi0 SIZE
- Cloud-init section (caldera-vm uses cloud-init; Kali does NOT — skip cloud-init entirely for Kali)

From scripts/proxmox/provision-windows.sh (NIC convention):
- net0 = vmbr1 (TARGET 10.10.10.0/24)
- net1 = vmbr0 (MGMT  10.0.0.0/24)
- Kali follows same dual-NIC convention

VMID assignments (from provision-windows.sh header + 03-CONTEXT.md D-01):
- dc01=201, sql01=202, exchange01=301, ws01=401, ws02=402, kali=601

IP assignments (from 01-CONTEXT.md D-NEW-07/D-NEW-08):
- Kali MGMT: 10.0.0.16/24 (vmbr0/net1), gw 10.0.0.1
- Kali TARGET: 10.10.10.200/24 (vmbr1/net0), no gateway

Locked specs (03-CONTEXT.md D-01):
- VMID: 601, Host 6 (alongside caldera-vm VMID ~600)
- RAM: 4096 MiB (4 GB per D-NEW-09 Host 6 layout: caldera-vm 6 GB + kali 4 GB = 10 GB)
- Disk: 80 GB (per D-NEW-09 Host 6 layout)
- Kali default credentials: kali/kali (must change before clean_state snapshot)

Critical difference from caldera-vm: Kali qcow2 is a FULL DESKTOP image with NO cloud-init.
Network config requires console session after first boot (03-RESEARCH.md Pitfall 3, Pattern 2).
Do NOT add --ipconfig0 or --cicustom flags. Do NOT add --ide2 cloudinit drive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write create-kali-vm.sh</name>
  <files>scripts/proxmox/create-kali-vm.sh</files>
  <read_first>
    - scripts/proxmox/create-caldera-vm.sh (template — copy qm create/importdisk/set/resize pattern; OMIT cloud-init section entirely)
    - .planning/phases/03-full-telemetry-reset/03-CONTEXT.md (D-01 locked specs: VMID 601, 4 GB RAM, 80 GB disk, dual-NIC, Host 6)
    - .planning/phases/03-full-telemetry-reset/03-RESEARCH.md (Pattern 2 for qm importdisk; Pitfall 3 explains why cloud-init is absent)
  </read_first>
  <action>
    Create scripts/proxmox/create-kali-vm.sh modeled on create-caldera-vm.sh with these differences:

    Argument signature: VMID LAN_GW STORAGE KALI_IMAGE SSH_KEYFILE
    (KALI_IMAGE is path to the downloaded kali-linux-YYYY.X-qemu-amd64.qcow2 file on Host 6)

    Locked specs (per D-01, D-NEW-09):
    - VM_NAME="kali"
    - VM_MEMORY=4096 (4 GB — Host 6 has caldera-vm 6 GB + kali 4 GB = 10 GB total)
    - VM_CORES=2
    - VM_DISK_SIZE="80G"
    - VMID locked at 601 in header comment; passed as argument for safety
    - net0: virtio,bridge=vmbr1 (TARGET NIC — 10.10.10.200 configured inside VM post-boot)
    - net1: virtio,bridge=vmbr0 (MGMT NIC — 10.0.0.16 configured inside VM post-boot)

    qm create step includes: --name --memory --cores --cpu cputype=host --net0 "virtio,bridge=vmbr1" --net1 "virtio,bridge=vmbr0" --ostype l26 --agent 1 --onboot 1 --scsihw virtio-scsi-pci

    After qm create, use qm importdisk to import the qcow2, then qm set to attach as scsi0 boot disk, then qm resize to 80G.

    OMIT entirely: --ide2 cloudinit, qm set --ciuser/--sshkeys/--ipconfig0/--nameserver/--cicustom. Kali does not use cloud-init. Add a prominent comment block explaining this distinction.

    Add a confirmation prompt before any qm commands (same pattern as create-caldera-vm.sh).

    Script header must include:
    - LOCKED SPECS comment referencing D-01 and D-NEW-09
    - Network config note: "IPs configured manually via Proxmox console after first boot (Pitfall 3 — no cloud-init)"
    - Explicit download URL for Kali qcow2: "https://www.kali.org/get-kali/#kali-virtual-machines → QEMU"
    - Reminder: "Default credentials: kali/kali — MUST be changed before clean_state snapshot (RESET-01)"
    - D-02 note: BloodHound CE and Mimikatz staging run inside Kali in Task 3, not via this script

    Summary section after qm commands must print next steps:
    1. qm start 601 on Host 6
    2. Open Proxmox console for VM 601 → log in as kali/kali
    3. Configure network interfaces (instructions in Task 2 checkpoint)
    4. Verify SSH from elastic-vm: ssh kali@10.0.0.16
  </action>
  <verify>
    <automated>grep -c 'VMID=601\|vmid.*601\|601' /home/researcher/Research/titulacion/scripts/proxmox/create-kali-vm.sh</automated>
  </verify>
  <done>
    create-kali-vm.sh exists; contains VMID 601; contains qm importdisk; no cloud-init flags present (grep for "cloudinit" returns 0 matches); contains net0 vmbr1 and net1 vmbr0; set +x executable bit
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Run create-kali-vm.sh on Host 6 + configure Kali network via console</name>
  <what-built>create-kali-vm.sh written in Task 1. This task runs it on Host 6 and configures Kali's static IP addresses via the Proxmox console, since Kali qcow2 does not support cloud-init (03-RESEARCH.md Pitfall 3).</what-built>
  <how-to-verify>
    Step 1 — Download Kali qcow2 if not already present on Host 6:
      Visit https://www.kali.org/get-kali/#kali-virtual-machines
      Select "QEMU (64-bit)" → download kali-linux-YYYY.X-qemu-amd64.qcow2
      Copy to Host 6: scp kali-linux-*.qcow2 root@host6:/var/lib/vz/template/iso/

    Step 2 — Run the creation script on Host 6:
      ssh root@host6
      bash /path/to/create-kali-vm.sh 601 10.0.0.1 local-lvm \
        /var/lib/vz/template/iso/kali-linux-YYYY.X-qemu-amd64.qcow2 \
        /root/.ssh/id_rsa.pub
      qm start 601

    Step 3 — Configure static IPs via Proxmox console:
      In Proxmox web UI → Host 6 → VM 601 → Console (or: ssh root@host6 "qm monitor 601")
      Log in with: kali / kali
      Run the following (adapt interface names if ens18/ens19 differ — use "ip link" to list):

      sudo nmcli connection add type ethernet ifname eth0 con-name TARGET ip4 10.10.10.200/24
      sudo nmcli connection add type ethernet ifname eth1 con-name MGMT ip4 10.0.0.16/24 gw4 10.0.0.1
      sudo nmcli connection up TARGET
      sudo nmcli connection up MGMT

      OR if using /etc/network/interfaces (ifupdown):
      Edit /etc/network/interfaces to add:
        auto eth0
        iface eth0 inet static
          address 10.10.10.200
          netmask 255.255.255.0
        auto eth1
        iface eth1 inet static
          address 10.0.0.16
          netmask 255.255.255.0
          gateway 10.0.0.1
      Then: sudo systemctl restart networking

    Step 4 — Verify MGMT IP reachable from elastic-vm:
      From elastic-vm (10.0.0.10): ping -c 3 10.0.0.16
      Expected: 3 packets received, 0% packet loss

    Step 5 — Change default credentials (required before clean_state snapshot per RESET-01):
      In the Kali console: passwd kali
      Set a strong password (record in .planning/secrets/PASSWORDS.md under "kali-vm")

    Step 6 — Enable SSH on Kali (if not already enabled):
      sudo systemctl enable ssh && sudo systemctl start ssh

    Step 7 — Set up SSH key from elastic-vm to Kali:
      From elastic-vm: ssh-copy-id kali@10.0.0.16
      Verify: ssh kali@10.0.0.16 "hostname && ip addr show"
      Expected output: "kali" and IPs 10.0.0.16 + 10.10.10.200

    Step 8 — Establish elastic-vm SSH keys to all Proxmox hosts (required for reset_range.sh fan-out per D-17/D-18):
      From elastic-vm, run for each host:
        ssh-keygen -t ed25519 -C "elastic-vm-reset-fanout" -f ~/.ssh/id_ed25519_proxmox (if not already done)
        ssh-copy-id -i ~/.ssh/id_ed25519_proxmox.pub root@host2
        ssh-copy-id -i ~/.ssh/id_ed25519_proxmox.pub root@host3
        ssh-copy-id -i ~/.ssh/id_ed25519_proxmox.pub root@host4
        ssh-copy-id -i ~/.ssh/id_ed25519_proxmox.pub root@host6
      Verify each: ssh root@host2 "qm list | head -3"
                   ssh root@host3 "qm list | head -3"
                   ssh root@host4 "qm list | head -3"
                   ssh root@host6 "qm list | head -3"
      Expected: qm list output showing dc01/sql01, exchange01, ws01/ws02, kali respectively

    Step 9 — Add host aliases to elastic-vm /etc/hosts (required so reset_range.sh uses hostnames not IPs):
      On elastic-vm, append to /etc/hosts:
        <host2-actual-IP>  host2
        <host3-actual-IP>  host3
        <host4-actual-IP>  host4
        <host6-actual-IP>  host6
      Verify: ssh root@host2 hostname && ssh root@host3 hostname && ssh root@host4 hostname && ssh root@host6 hostname

    Step 10 — Install BloodHound CE and stage Mimikatz (per D-02):
      SSH into Kali: ssh kali@10.0.0.16

      Install docker + docker-compose if not present:
        sudo apt-get update && sudo apt-get install -y docker.io docker-compose
        sudo systemctl enable docker && sudo systemctl start docker
        sudo usermod -aG docker kali

      Install BloodHound CE (SpecterOps):
        mkdir -p ~/bloodhound && cd ~/bloodhound
        curl -L https://ghcr.io/specterops/bloodhound/docker-compose -o docker-compose.yml
        sudo docker compose up -d
        (BloodHound CE UI accessible at http://10.0.0.16:8080 — configure admin credentials on first login)

      Stage Mimikatz Windows PE at /opt/mimikatz/ (per D-02):
        sudo mkdir -p /opt/mimikatz
        Download or copy mimikatz_trunk.zip from the mimikatz GitHub releases page
        sudo unzip mimikatz_trunk.zip -d /opt/mimikatz/
        ls /opt/mimikatz/x64/mimikatz.exe  (verify binary present)

    Step 11 — Verify INFRA-07 tools present:
        msfconsole -q -x "version; exit" 2>&1 | grep "Framework"
        impacket-smbclient --version 2>&1 | head -1
        nmap --version | head -1
        ls /opt/mimikatz/x64/mimikatz.exe

    Expected: each command returns version info without "command not found"
  </how-to-verify>
  <resume-signal>Type "kali-ready" when: SSH from elastic-vm reaches 10.0.0.16 without password, all 4 Proxmox hosts accept passwordless SSH from elastic-vm, BloodHound CE docker-compose is up, and mimikatz.exe is at /opt/mimikatz/x64/</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| elastic-vm → Proxmox hosts (SSH root) | Passwordless root SSH used for qm rollback/snapshot operations |
| elastic-vm → Kali (SSH kali) | SSH key enrollment for agent management and enrollment scripts |
| Kali → TARGET subnet (vmbr1) | Kali is the attacker platform; all emulation traffic originates here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Kali default credentials kali/kali | mitigate | Change password before clean_state snapshot (Task 2 Step 5); verified by accept criteria |
| T-03-02 | Elevation of Privilege | elastic-vm root SSH to all Proxmox hosts | accept | MGMT network is isolated (no internet, lab-only); root SSH is required for qm commands; SSH key not password |
| T-03-03 | Information Disclosure | Kali SSH private key on elastic-vm | mitigate | ed25519 key stored only in elastic-vm ~/.ssh; never committed to git |
| T-03-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No package install in this plan (Docker from apt, BloodHound CE from ghcr.io official image) |
</threat_model>

<verification>
From elastic-vm after Task 2 completes:

1. Kali reachable:
   ssh kali@10.0.0.16 "hostname && ip addr | grep '10\\.0\\.0\\.16\|10\\.10\\.10\\.200'"

2. Proxmox host SSH fan-out works:
   for h in host2 host3 host4 host6; do echo -n "$h: "; ssh root@$h "qm list | wc -l"; done

3. VMID 601 exists on Host 6:
   ssh root@host6 "qm config 601 | grep -E '^name|^memory|^net'"
   Expected: name: kali, memory: 4096, net0 bridge=vmbr1, net1 bridge=vmbr0

4. BloodHound CE running:
   ssh kali@10.0.0.16 "sudo docker ps | grep bloodhound"

5. Tools present:
   ssh kali@10.0.0.16 "which msfconsole impacket-smbclient nmap && ls /opt/mimikatz/x64/mimikatz.exe"
</verification>

<success_criteria>
- VMID 601 (kali) exists on Host 6; qm config 601 shows memory: 4096, net0 vmbr1, net1 vmbr0
- ssh kali@10.0.0.16 from elastic-vm succeeds without password
- ssh root@host2/host3/host4/host6 from elastic-vm succeeds without password; qm list returns output
- BloodHound CE docker-compose stack is up (docker ps shows bloodhound containers)
- /opt/mimikatz/x64/mimikatz.exe exists on Kali
- msfconsole, impacket-smbclient, and nmap return version strings (not "command not found")
- Kali default password changed (kali/kali no longer valid)
</success_criteria>

<output>
Create `.planning/phases/03-full-telemetry-reset/03-01-SUMMARY.md` when done
</output>
